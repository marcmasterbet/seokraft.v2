const btn=document.querySelector('.menu-btn'),links=document.querySelector('.nav-links');if(btn&&links){btn.addEventListener('click',()=>{const o=links.classList.toggle('open');btn.setAttribute('aria-expanded',String(o))});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('open');btn.setAttribute('aria-expanded','false')}))}const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));const form=document.querySelector('#contactForm'),status=document.querySelector('#formStatus');if(form&&status){form.addEventListener('submit',async e=>{e.preventDefault();const n=form.elements.name,p=form.elements.phone,b=form.querySelector('button[type=submit]');if(!n.value.trim()||!p.value.trim()){status.textContent='Merci d’indiquer votre nom et votre téléphone.';status.style.display='block';status.style.color='#ff8a3d';(!n.value.trim()?n:p).focus();return}const t=b.textContent;b.disabled=true;b.textContent='Envoi…';try{const r=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!r.ok)throw new Error();form.reset();status.textContent='Merci. Votre demande a bien été envoyée.';status.style.color='#67ff4f';status.style.display='block'}catch{status.textContent='L’envoi a échoué. Vous pouvez réessayer dans un instant.';status.style.color='#ff8a3d';status.style.display='block'}finally{b.disabled=false;b.textContent=t}})}